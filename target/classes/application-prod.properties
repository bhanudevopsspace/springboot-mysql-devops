# Production profile - use secure credentials and production DB (hard-coded for now)
# Production database (hard-coded values) - replace with real hosts/creds as needed
spring.datasource.url=jdbc:mysql://prod-db.example.com:3306/proddb
spring.datasource.username=produser
spring.datasource.password=prodpass

# Production runtime settings
server.port=8080
spring.jpa.show-sql=false
spring.jpa.hibernate.ddl-auto=validate

logging.level.root=INFO
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=never

# Security note: never commit real credentials to version control. Replace with secrets manager in CI/CD.
